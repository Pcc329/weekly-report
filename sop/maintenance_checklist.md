# Weekly-Report 內容維護檢查清單

**建立日期**：2026-08-25
**目的**：weekly-report網站裡有些內容會隨時間過期（數字、狀態標籤），這份清單列出所有已知的「會過期」位置，之後要更新時照著這份清單逐項核對，不用每次重新盤點。

**使用方式**：每次要做內容更新時，從上到下過一遍，確認每一項是否仍然正確；不正確的就更新，正確的就跳過。

---

## 🔴 會過期的內容（需定期檢查）

### index.html — 開發進度分頁

| 位置 | 內容性質 | 檢查方式 |
|---|---|---|
| 「案例分析功能開放測試提案」卡片 `card-desc` | 案例筆數（目前寫「69筆真實案例」） | 對照 `SELECT COUNT(*) FROM cases WHERE is_real = true;` 的實際數字 |
| 「測試回報表單」卡片 `card-badge` | 開放測試狀態標籤（目前「已暫緩」） | 確認開放測試是否有新進展，狀態是否需要改回「試行中」或「已完成」 |

### index.html — 最新動態（本週摘要／待決策事項）

| 位置 | 內容性質 | 檢查方式 |
|---|---|---|
| `ws-date` 本週摘要日期 | 每次有重大進度時應更新 | 確認顯示日期是否為最近一次重大更新的日期 |
| `ws-conclusion` 摘要結論文字 | 一句話總結近況 | 每次工作階段性完成時重寫 |
| `ws-item` 摘要清單4項 | 本週/本階段完成的具體項目 | 對照實際完成的任務更新 |
| `decision-item` 待決策事項清單 | 目前3項 | 已解決的移除，新出現的待決策事項加入 |

### index.html — 資料新鮮度稽核區塊

| 位置 | 內容性質 | 檢查方式 |
|---|---|---|
| `log-item` 三筆稽核紀錄卡片 | 每完成一輪稽核就新增一筆 | 對照 `freshness-audit-log.md` 是否有新段落還沒同步進index.html |
| 錨點連結 `href="freshness-audit.html#xxx"` | 每筆卡片需對應log檔案裡的正確錨點id | 新增稽核記錄時，log檔案跟index.html的錨點id要同步建立 |

### freshness-audit-log.md

| 位置 | 內容性質 | 檢查方式 |
|---|---|---|
| 每輪稽核段落 | 累積式增加，不刪除舊內容 | 新增段落記得加`<a id="...">`錨點，並在index.html對應新增連結 |
| 待辦清單（每個日期段落結尾） | 反映當下未解決事項 | 已完成的用刪除線標記＋補充完成日期，不要直接刪掉整行 |

### status.md

| 位置 | 內容性質 | 檢查方式 |
|---|---|---|
| 每日記事 | 逐日新增，不覆蓋舊內容 | 確認每次都是新增一則，不是編輯到舊的那則裡 |

---

## 🟡 需要人工判斷是否過期（不是單純數字，需要你評估現況）

| 位置 | 內容 | 上次確認日期 |
|---|---|---|
| 「資料庫升級路線圖」卡片描述「現況校準 8/7」 | 是否需要重新校準內容 | 2026-08-07（提及於文案中） |
| 開放測試提案的整體策略定位 | 是否還適合放在「開放測試」分類下，還是該改成別的敘事框架 | 待PPC決定 |

---

## 🟢 不會過期的內容（確認過，不用管）

- 方法論分頁各文件卡片的建立日期標籤（例如`2026-07-16`）——這是文件建立時間戳記，本質是歷史記錄，不是即時狀態宣稱
- 「切換完成報告」卡片寫的「對外2,463」——這是8/12切換當下的快照數字，屬於歷史記錄
- dashboard.html的KPI數字（總方案數、AI方案數等）——即時API抓取，自動更新，不在weekly-report檔案裡，不用手動維護

---

## 建議的檢查頻率

- **每次完成一輪資料新鮮度稽核**：更新「資料新鮮度稽核區塊」+「本週摘要」
- **每週五封存週報時**：順便過一次這份清單的🔴區塊
- **每月或重大進度時**：檢查🟡區塊是否需要人工判斷更新
