# Codex 規格書範本（通用）v2.1

> 使用說明：每次新任務複製這份，填入 `【】` 區塊，刪掉用不到的段落。
> 灰字註解（> 開頭）交付前可刪除。
>
> **v2 異動說明（2026-08-18）**：新增第五之二、五之三段，及第六段結構化驗證聲明。
> 起因為 PR #126 事故——Supabase select 語句加入不存在的欄位，導致正式站
> `/api/solutions` 全面 500。詳見 `_handover/incident_SOP_20260818.md`。
>
> **v2.1 異動說明（2026-08-18 追加）**：新增第四之二段「優雅降級（單欄失敗
> 容錯）」。同一次事故的延伸檢討——除了「動工前確認欄位存在」，也應要求
> 查詢邏輯本身具備「單一欄位失敗不拖累其他欄位」的容錯設計（業界標準名稱：
> 優雅降級 Graceful Degradation／容錯隔離 Fault Isolation）。

---

## 任務名稱：【簡短描述本次任務】

**日期**：【YYYY-MM-DD】
**提出**：Claude
**目標**：【一句話說明這次要達成什麼，以及最重要的限制條件】

---

## 一、背景（為什麼做這件事）

> 讓 Codex 理解脈絡，避免它自作主張。簡述現況問題 + 為何選這個做法。

【背景說明】

> ⭐若本任務是「恢復／新增顯示某欄位」，提出規格書前先確認該欄位在受影響
> 資料範圍內的非空值比例（填寫率）。若填寫率過低（例如 <20%），先與人類
> 確認這是否該轉為資料補值任務，而非顯示邏輯任務。
>
> 範例 SQL：
> ```sql
> select count(*) filter (where industry_category is not null) as filled,
>        count(*) as total
> from solutions
> where data_source = '【篩選條件】';
> ```
> 反例（PR #126 教訓）：當時直接補回 `solution_category` 的顯示邏輯，
> 事後才發現受影響的農業部方案該欄位在 Airtable 裡本來就是 NULL——
> 不是命名問題，是資料從未被分類過。若先查過填寫率，這件事在動工前
> 就能被攔下來，轉成 ETL 補值任務排進 backlog，而不是先寫顯示程式碼
> 才發現白做工。

---

## 二、需求（要改什麼）

> 逐項列出。每項盡量寫成「現況 → 需求」的對照，越具體越好。

### 改動項目 1：【名稱】
- 現況：【目前怎樣】
- 需求：【要變成怎樣】
- 涉及檔案：【api/xxx.js、public/xxx.html】

### 改動項目 2：【名稱】
- 現況：
- 需求：
- 涉及檔案：

---

## 三、驗收標準

> 寫成「可勾選」的條件，Codex 才知道做到什麼程度算完成。

- [ ] 【條件 1，例如：result_count 不變】
- [ ] 【條件 2，例如：top5 排序隨產業不同】
- [ ] 【條件 3】

---

## 四、不要動的東西（防呆）

> 這段是避免 Codex 越界的關鍵，務必填寫。常見禁區：

- 不得修改 `/api/solutions` 語意搜尋核心邏輯
- 不得新增硬性篩選條件（避免 0 缺口）
- 不得改動 Airtable 資料結構（除非本任務明確需要）
- 【其他本次特定禁區】

> ⭐若本任務涉及新增或修改 Supabase select 語句中的欄位，**執行前**先比對
> `_handover/supabase_schema_snapshot.json`，確認欄位確實存在於清單中；
> 若清單裡沒有該欄位，先在 SYNC 中標記「欄位未在 snapshot 中找到，
> 需人工二次確認」，不要假設欄位存在並直接寫入 select 語句。
>
> snapshot 檔案若不存在或明顯過期（例如超過 1 個月未更新），視同
> 「無法比對」，比照下方第五之二段處理——不得省略欄位驗證步驟。

---

## 四之二、優雅降級（單欄失敗容錯）⭐視任務規模決定是否強制，建議所有新增顯示欄位任務納入⭐

> 此段與五之二（欄位存在性驗證）互補：五之二是「動工前先確認欄位存不存在」，
> 這段是「萬一還是漏判、或未來欄位被異動，不要讓一個欄位的問題拖累整份回應」。
> 兩者都做，才是真正的防呆。

- 若本任務涉及在既有的整批資料回應中（例如一次查詢回傳多個欄位）新增欄位，
  該欄位的讀取**必須做成獨立、可個別失敗的區塊**，不得讓它與其他既有欄位
  綁在同一個「全部成功才算成功」的查詢或回應邏輯裡。
- 新欄位讀取失敗時的預期行為：該欄位顯示為空值或預設文字（例如「暫無資料」），
  **其餘既有欄位不受影響、正常顯示**。
- 驗收標準應包含一項反向測試：**刻意讓新欄位讀取失敗**（例如暫時改成一個
  不存在的欄位名稱），確認其餘既有欄位與整體功能仍正常運作，不會連帶回傳
  錯誤或整體失效。

> 對照 PR #126 事故：當時 `solution_category` 這一個不存在的欄位，
> 拖垮了同一支查詢裡 `data_source`、`price` 等其他完全正常的欄位，
> 導致整支 API 全面回傳錯誤。若查詢邏輯當初採用單欄獨立處理（優雅降級／
> Graceful Degradation），這次事故的影響範圍會被限制在「新欄位顯示空白」，
> 不會演變成正式站全面故障。

---

## 五、驗證資料規範 ⭐固定保留⭐

> 這段一律保留，避免 Codex 抓到歷史快取檔做離線驗證。

- 離線驗證**一律使用本次附上的最新 CSV**，勿使用任何歷史日期的快取檔案。
- 本次驗證資料檔名：【Solutions-Grid_view_YYYY-MM-DD.csv】
- 若本次無附 CSV，請以線上 API 即時資料為準，並在 SYNC 中註明驗證來源與資料筆數。
- 驗證結果須標明「驗證時的資料總筆數」，供 review 對照現況。

---

## 五之二、Supabase 欄位存在性驗證 ⭐固定保留⭐

> 凡本任務涉及修改 `api/*.js` 中 Supabase select 語句（新增、更名、
> 移除欄位皆算），此段必填，擇一勾選並附證據，**不得留白**。

- [ ] 已用以下 SQL 在 Supabase SQL Editor 執行，並附上輸出結果：
  ```sql
  select column_name, data_type
  from information_schema.columns
  where table_name = '【表名】'
  order by ordinal_position;
  ```
- [ ] 已在真實 Preview 環境（非 mock）呼叫過該 API，附上：
  - HTTP 狀態碼（須為 200）
  - 回傳 JSON 前 3 筆內容截圖或文字貼上

> **只用 mock 資料測試視為未完成本項，不得作為 merge 依據。**
> 此為 PR #126 事故直接教訓——當時 Supabase `solutions` 表沒有
> `solution_category` 欄位，PostgREST 回傳 `42703 column does not
> exist`，因 mock 測試無法檢查真實 schema 而未被攔下，導致正式站
> `/api/solutions` 全面 500。

---

## 五之三、驗證方式聲明 ⭐固定保留、必填、只能勾一項⭐

- [ ] 已在真實 Preview／正式環境驗證，證據見上方第五之二段
- [ ] 僅完成 mock／靜態驗證，**尚未真實環境驗證**

> ⚠️ 若勾選第二項，此 PR 狀態為「**未完成驗收**」。收到此 SYNC 後，
> Claude 與人類 reviewer 的下一步**只能是**「安排真實環境驗證」，
> **不得**直接建議或執行 merge，即使程式碼 diff 看起來合理、
> 即使未修改任何危險函式。
>
> 若受限於執行環境（如網路規則封鎖、無法連線 Vercel），不能自行
> 完成真實環境驗證，必須在此明確寫出「本環境無法驗證，待人工在
> 有連線權限的環境二次確認」，並暫停此 PR 的 merge 建議，直到
> 該確認完成為止。

---

## 六、回傳要求（SYNC .md 須包含）

> 固定保留，確保 SYNC 內容足以 review。

1. 實際改動的檔案與函式清單
2. 關鍵邏輯的實作位置與參數設定
3. 至少【N】組驗證範例（含前後對比）
4. 確認驗收標準逐項達成
5. Git 資訊：branch / commit / PR 連結
6. 驗證所用資料來源與筆數
7. 第五之二段（Supabase 欄位存在性）與第五之三段（驗證方式聲明）
   逐項填寫結果，不得省略
8. 若本任務涉及新增顯示欄位，第四之二段（優雅降級）的反向測試結果
   （刻意讓新欄位失敗、確認其餘欄位不受影響）

---

*範本由 Claude 產出 · 版本 v2.1（2026-08-18）· 沿革：v1（2026-06-03）→ v2（2026-08-18）*
