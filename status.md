# 2026-09-07

## 一、資服產業資料庫：資料來源頁與規模顯示收尾

1. **資料來源說明頁新增9個官方網站相關連結**，多網址來源（雲市集工業館3個、新北產業AI化輔導2個）統一用下拉選單呈現，臺灣雲市集因確認為已停止服務公告頁，不放連結改加註提示文字
2. **修正過程中發現一次預設狀態顯示bug**：下拉選單CSS的`display: grid`覆蓋了HTML`hidden`屬性的預設隱藏效果，導致頁面載入即為展開狀態，已修正並實測收合正常
3. **SMEI錯字全站修正為「SME AI平台」**，對應同仁組長回饋

<details>
<summary>展開細節</summary>

連結來源比對`data_sources`表（13筆，含url、稽核狀態欄位），發現3筆「尚未稽核」來源，透過Colab notebook實測HTTP存活狀態後確認皆可正常連線。臺灣雲市集雖回應200，但頁面內容為服務終止公告（已截圖確認），維持既有稽核結論不放連結。順帶發現`data_sources`表`organizer`欄位混用「主辦單位」與「執行單位」兩種角色，記錄為待辦，本次不處理。

PR：#143（連結新增）、後續2次追加commit（按鈕視覺區分、預設收合bug修正）

</details>

## 二、資安補強：CIA框架盤點與4項低成本修補上線

1. **組長提案「網站是否會被搜尋到」，經評估有修補的必要，回頭比對7/13資安基線報告**，確認為報告已列管、但兩個月未處理的已知風險（API無身分驗證）的另一個面向
2. **用CIA框架（機密性/完整性/可用性）重新盤點現況**，完成4項低成本、可獨立處理的補強，全部合併上線
3. **過程中新發現一項原本未列管的風險並修補**：AI搜尋/AI分析功能的回答內容轉換為HTML顯示時未經消毒，理論上有機會被注入可在使用者瀏覽器執行的程式碼
4. **查證確認一項風險為虛驚**：後端service-role金鑰全站掃描後確認未外流至前端可讀取檔案
5. **最大缺口（API身分驗證）本次未處理**，7/13報告已列管至今仍未執行，判斷為需獨立規劃的大工程，不與本次小額補強混合處理

<details>
<summary>展開細節</summary>

### 本次完成的4項（PR #144-146）

| 項目 | CIA面向 | 內容 | PR |
|---|---|---|---|
| AI回答XSS消毒 | 完整性 | `index.html`、`compare.html`的`marked.parse()`輸出改為經`DOMPurify.sanitize()`消毒後再渲染，`manufacturing.html`查證後確認已用`escapeHtml`正確處理，不受影響 | #144 |
| robots.txt / noindex | 機密性 | 新增robots.txt，8支頁面全數加noindex meta tag，降低搜尋引擎索引曝露機率；不影響知道網址者直接存取 | #145 |
| feedback端點流量管制 | 可用性 | `api/feedback.js`加上簡易IP速率限制（每分鐘最多3次），實測第4次起正確回傳429；限制僅單一Serverless執行個體內有效，已知限制記錄於SYNC文件 | #146 |
| service-role金鑰洩漏查證 | 機密性 | 全站`grep`掃描`public/`目錄，確認金鑰僅存在於`api/`後端程式，結果乾淨 | 無需PR |

### 全站寫入端點盤點（完整性稽核附帶發現）

確認全站僅`api/feedback.js`一支寫入端點，本次一併補上流量管制。

### 現況仍未解決的缺口

- API無身分驗證（機密性最大缺口，7/13報告已列管）
- 欄位白名單為唯一防線（RLS對後端無效，因service-role金鑰性質為繞過RLS）
- 備份可還原性從未實際演練（完整性）
- 無監控告警機制，依賴人工發現異常（可用性）

### 產出文件

- 決策性文件：`資安補強計畫_CIA框架_2026-09-07.md`（完整優先序排列）
- 對外報告：`report_security_hardening_20260907.html`，已發布至weekly-report站`docs/`資料夾，並於`index.html`「資訊安全」區塊新增卡片連結

</details>

## 三、方法論收穫

1. **「多頁面共用同一顯示邏輯」的排查習慣，本次延續生效**：處理sources.html連結時遇到的CSS覆蓋hidden屬性問題，透過使用者實測（而非僅憑程式碼審查）才發現，再次驗證「拉實際結果核對、不只看diff」的必要性
2. **資安問題的優先序判斷原則**：同一輪盤點會同時出現「大洞」與「小洞」，先處理低成本、可獨立完成的項目，大工程（API身分驗證）另外排程規劃，避免任務範圍失控——此原則由使用者於本次討論中主動提出並確認

---

# 2026-09-08

## 一、API 身分驗證 Phase 1 上線

過去系統就像一棟辦公大樓完全沒裝門——不管是內部同仁還是路過的陌生人，只要知道網址，能做的事情一模一樣，都能直接把整個資料庫的內容整包搬走。這次做的事，簡單說就是幫大樓裝上一道門，進門要刷卡（輸入共用密碼）。

**重點：**
- 防的不是「駭客攻擊」，是「任何人知道網址就能繞過網頁畫面，直接抓走全部資料」這件事
- 內部同仁**只需要登入一次**，7天內不用重複輸入密碼，體驗上跟以前差不多
- 沒有密碼的外部瀏覽器或程式，一律擋在門外（技術上是回傳401錯誤）
- weekly-report 首頁的統計數字、Colab 的資料處理腳本，都不受這次改動影響，照常運作
- 這只是「先求有」的第一步——目前是全部同仁共用一組密碼，還分不出「現在是誰在用」；未來若需要更精細管理（例如區分誰能看什麼），會是下一階段獨立規劃的事

<details>
<summary>技術細節</summary>

- PR #147（`feat/api-auth-phase1-2026-09-08`），已 merge
- 新增 `_lib/auth.js`（session HMAC-SHA256 驗證 + API Key allowlist 預留位）、`api/auth.js`（login/logout/session-check，經 vercel.json rewrite 對外維持原路徑）、`public/auth-gate.js`（前端登入遮罩）
- 9 支既有 API 端點（solutions/companies/cases/ask/claude/company-detail/feedback/manufacturing-analyze/manufacturing-recommend）加上驗證檢查，`api/stats.js` 維持公開（因應 weekly.html 匿名呼叫）
- 8 支前端頁面（含 diagnosis.html）加入登入遮罩，一致性優於保留例外
- 已於 Vercel 設定 `SITE_PASSWORD`、`SESSION_SECRET` 兩組環境變數（Production + Preview），密碼另行通知內部同仁，不記錄實際值
- Codex 全程未經手任何密鑰實際值，登入成功流程由 PPC 於 Preview 環境人工驗收通過
- 預留 `API_KEYS_ALLOWLIST` 機制供未來機器對機器呼叫（如 Rio 的資服業資料庫）使用，目前未發放任何實際 Key
- 待辦：個人帳號登入（Phase 2）獨立規劃，未排入本輪

</details>

## 二、內部通知已發出

已寄出第一封公告信（說明新增登入機制、不含密碼），密碼將於第二封信單獨發送，避免密碼與通知內容同信流通。

---

# 2026-09-09

## 一、Rio專案（資服業者領域能量資料庫）兩項收尾確認

1. **`資服業landscape0607.xlsx`確認為已收尾狀態**：本會在8/31-9/3已完成根因診斷、產業資料庫提案、2,487筆Solutions資料交付，PPC角色為提供資料與判斷依據，執行主體是該專案PM與團隊。當初查出的統編異常（84499283對到3家公司、24231451對到2家公司）9/2已查證並記錄，非新發現。提案備而待用，重啟時機取決於9/11 DEMO後的評估結果
2. **貼標與搜尋排序DEMO（9/11）狀態修正**：Rio為該專案負責人，本會配合工作已結束，改列為追蹤狀態，不需主動推進

## 二、`triage_缺_26筆.csv`異常方案稽核：判定完成

透過Supabase實際查詢核對26筆的`record_status`，確認25筆判定邏輯與現有程式碼過濾標準（`已下架_資料異常`）一致，可直接信任執行；發現1筆例外（SOL-0906）狀態已於稽核清單產生後被更新為`已下架_公司歇業佐證`，判定由「優先修復」修正為「維持下架」。

最終分類：優先修復14筆、例外（維持下架）1筆、可下架3筆、下架候選（空殼）8筆，合計26筆核對無誤。

<details>
<summary>展開細節</summary>

**執行狀態**：本次僅完成判定分類，實際資料庫寫入動作（下架11筆、排入修復排程15筆）尚未執行，待排入下次工作。

**方法論**：稽核清單的`verdict`欄位判定依據，透過比對Supabase `record_status`實際分布（`已下架_資料異常`25筆、`已下架_公司歇業佐證`13筆等5種狀態），確認稽核範圍刻意排除了`已下架_平台停止服務`（153筆）等原因明確、無討論空間的類別，僅鎖定有修復可能性的「資料異常」類別，數字對得起來（25+1=26）。

</details>

## 三、快速查證兩項：CSP第二波、重置按鈕行為

1. **CSP第二波（Content-Security-Policy）：確認尚未啟動**。`vercel.json`的headers清單有`X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy`、`Permissions-Policy`四項，唯獨沒有`Content-Security-Policy`，全站程式碼搜尋亦無相關實作痕跡。7/13報告規劃至今無程式碼層級進度，待排入規劃
2. **「重置」按鈕行為：確認非重新整理頁面**。`manufacturing.html`的`resetAgent()`函式是清空20餘個JavaScript狀態變數並重新渲染畫面，過程網頁不會重新載入；且因為同時清除`sessionStorage`，實際效果比單純重新整理更徹底。此項查證已完成，可直接劃記結案

## 四、公司Logo顯示功能上線（PR #148、#150）

過去方案列表跟詳情頁只有公司名稱文字，這次補上公司代表圖示——有真實logo的公司顯示圖片，沒有的公司顯示「名稱首字＋固定顏色」的縮寫圖示（類似LINE新聊天室的預設頭像邏輯），使用者不會看到任何「這是替代圖」的提示。

**重點：**
- 930家公司中446家（48%）透過Logo.dev API找到真實logo，其餘484家使用縮寫顯示
- 兩個月前測試「Clearbit 0%」的原因這次查清楚了：Clearbit Logo API已於2025/12正式關站，不是資料或技術問題；改用官方指定的繼任服務Logo.dev
- 縮寫圖示的顏色依公司名稱固定計算（hash），同一家公司在搜尋卡片、方案詳情頁、公司介紹分頁三處顯示的顏色完全一致，經實際畫面比對驗證通過
- `diagnosis.html`確認為`manufacturing.html`現行AI顧問功能的廢案前身，全站無內部連結指向，本次功能排除在外，另案處理下架（導向至manufacturing.html），列入待追蹤

<details>
<summary>技術細節</summary>

- Supabase `companies`表新增`logo_url`／`logo_checked_at`／`logo_source`三欄位，一次性Colab批次工作完成撈取，`logo_source='manual'`的記錄受保護不被批次覆寫
- 新增共用前端函式`public/company-avatar.js`（`renderCompanyAvatar`），內建XSS跳脫處理與URL合法性驗證，圖片載入失敗時`onerror`自動退回縮寫顯示
- `api/solutions.js`、`api/company-detail.js`皆新增`logo_url`回傳
- `manufacturing.html`「公司介紹」分頁原本存在一套獨立的舊配色邏輯（`getCompanyAvatarColor`，6組寫死hex色碼），本次統一改用共用函式，舊函式與其CSS已完全移除，全站grep確認無殘留呼叫
- PR流程：#148（搜尋卡片）與#150（詳情頁+統一舊邏輯，因應#148當時尚未merge而產生的中間態PR #149已關閉不採用）皆已merge至main

</details>

## 五、畫面切換捲動位置歸零修復（PR #151）

使用者實際測試時發現：列表頁往下捲動後點進方案詳情頁，畫面停留在原本捲動的中段位置，而非從最上方開始。

**根因**：全站切換首頁/列表/詳情三種畫面靠單一狀態變數控制（非真實網址跳轉），觸發這個變數改變的入口分散在7個不同地方（搜尋、快速搜尋、分類瀏覽、瀏覽器上一頁、返回首頁按鈕、點方案卡片等），過去皆無捲動歸零機制。

**修法**：不逐一修改7個入口，改為只在該共用狀態變數上新增一個監控（`useEffect`），不論從哪個入口觸發畫面切換都自動生效，僅新增3行程式碼，PR #151已merge。

## 六、方法論收穫

1. **規格書引用的舊資訊要重新查證，不能只憑記憶**：寫API身分驗證規格書時，引用了已不存在於現有程式碼的函式名稱（`scoreSolution`等），事後查證才發現該保護清單已過時，予以修正並記錄「下次寫規格書前先查實際程式碼」的提醒
2. **PR間有相依關係時，Codex可能搶先動工**：新規格書交付當下，前一個相依的PR尚未merge，Codex仍以該PR分支為base繼續開發，避免了重複建立共用檔案的問題，但也產生了「合併後需retarget」的額外收尾步驟
3. **人工實測比自己以為的還重要**：這次的捲動bug是使用者自己操作時偶然發現，不是規格或程式碼審查能預先抓到的類型，再次印證「拉實際結果核對」原則不只適用於diff審查，也適用於實際使用情境的隨手測試

---

# 2026-09-10

## 一、備份還原演練：首次實測，通過

過去每週雖然都有備份，但從未真的測試過「這份備份能不能救回資料」。這次補跑上週漏跑的週備份後，直接拿它做了一次完整的還原演練。

- 補跑上週漏跑之週備份，存入 OneDrive
- 完成季度還原驗證演練，資料表筆數比對一致：companies 930/930、solutions 2487/2487、cases 115/115
- 過程排除三項技術障礙：
  - `psql.exe` 被 IT 應用程式控制原則封鎖 → 改用 Node.js + `pg` 套件執行還原
  - `pg_dump` 預設 COPY 格式無法被 `pg` 套件解析 → 備份指令加 `--inserts`
  - 還原檔含 `auth` schema 與自訂角色（`admin_role`/`consultant`）導致衝突 → 備份指令加 `--schema=public --no-owner --no-acl`，並於還原腳本過濾殘留 GRANT/POLICY/OWNER 指令
- 標準備份指令與還原腳本已寫入 `supabase-backup-drill` skill
- 測試專案歷經 v1→v2→v3，前兩版因指令未過濾 schema/owner/acl 而中途失敗，v3 一次成功
- `restore-drill-test-20260910-v3` 測試專案已刪除，演練收尾

## 二、26筆異常方案複查結案 + solution_reviews 軌跡機制

**與9/9判定的落差說明**：9/9稽核當時判斷「實際資料庫寫入動作尚未執行」，今天實際透過SQL查詢核對後發現，這26筆在Supabase裡**早已全數是已下架狀態**（來自2026-08-20的一支批次腳本統一標記），並非等待執行的待辦——9/9的判定邏輯本身沒錯，但「尚未執行」這個現況描述有誤，予以修正。

- 針對26筆逐一查詢`solution_id`、`airtable_rec_id`、`company_id`、`record_status`，確認25筆為`已下架_資料異常`、1筆（SOL-0906）為`已下架_公司歇業佐證`，與9/9判定的分類一致
- 15筆「唯一方案」（公司僅此一個方案）逐一查出完整欄位，確認為臺灣雲市集來源但缺乏實質內容（AI生成制式文案，price/website_url/target_industry皆空、score_overall僅2.1-2.6），無原始資料源可補，決定**維持下架、不修復**（原9/9判定為「優先修復」，本次決策推翻）
- 3筆「可下架」（公司有其他正常方案）：SOL-0242、SOL-0491、SOL-0772
- 8筆「空殼」（company_id為null）：維持下架候選

**新增審查軌跡機制**：已規格化貼給Codex，新增獨立資料表`solution_reviews`（review_id主鍵、solution_id外鍵，一對多設計），讓同一筆方案未來可被複查多次而不覆蓋歷史紀錄，26筆本次複查結果回填為第一批資料。規格書過程修正一次分類錯誤（SOL-0772曾誤放「唯一方案」，已改列「可下架」）。

- **待辦**：Codex執行結果尚未收到最終SYNC確認，下次接續時需先核對PR是否正確建表、26筆是否寫入無誤

## 三、9月移轉後重點盤點：分眾溝通產出

移轉滿一個月（8/12切換），依讀者分眾拆成兩份，不共用同一份內容：

- **PM/顧問適用（email）**：只保留會影響推薦操作的一項——臺灣雲市集來源方案已批次下架，建議改用其他來源；資料庫內部治理細節（如15筆異常方案下架，本已被搜尋API過濾不會被使用者看到）不放入
- **Alex/Carrie適用（完整版報告，docx + 週報站html雙版本）**：涵蓋移轉觀察期進度、備份可靠性驗證（本次還原演練結果）、資料品質稽核與清理成果（海巡稽核＋26筆triage）、安全稽核進度（PR#144-146已完成項目＋API身分驗證待處理）、下一步

## 四、週報站更新

- 新增`docs/migration_monthly_review_20260910.html`，收錄進「資料庫升級」分類，緊接在「資安補強執行報告」（9/7）卡片之後
- `index.html`新增對應nav-card，`資料庫升級`tab的tab-count由6修正為8（含本次新增，原本卡片數與計數本就有落差）
- **待辦**：兩個檔案內容都已備妥，PPC尚未實際commit到GitHub確認上線

## 五、專案最高原則設定

本次對話中設定「原則MD-1／MD-2」為產業資料庫專案最高原則：協助學習採家教式指導（給答案前先確認是否理解原理、舉例說明）；定期整理案例復盤，反向出案例考核使用者判斷。

---

## 待追蹤事項

| 項目 | 對象 | 預計時間 | 狀態 |
|---|---|---|---|
| 三張獨立 Table（能量登錄／補助／得標）實際資料量 | Rio Hsu | — | 已去信詢問，未回覆 |
| 貼標與搜尋排序 DEMO | Rio Hsu | 9/11 | 追蹤中，Rio為專案負責人，本會工作已結束 |
| 五個政策問題是否符合實際使用情境 | Carrie | — | 待提案啟用時確認 |
| S3 優先 3：全庫周邊表外鍵驗證 | 本會 | — | 未執行，非急迫 |
| SME AI 平台尋工具清單（約 279 筆） | 本會 | — | 待找出 JS 動態載入的 API 端點 |
| 農業雲市集數位館（93 筆） | 本會 | — | Cloudflare Turnstile 阻擋，需瀏覽器手動擷取 |
| 5 筆疑似已下架待人工複查（新創嚴選） | 本會 | — | 待確認是否真的下架 |
| 季河資訊、萬物智通兩組公司合併判斷 | 本會 | — | 證據不足，待補充 |
| Supabase Solutions 同步落差調查（約 14 筆） | 本會 | — | 獨立舊坑 |
| `index.html` 本週摘要日期顯示 08-25，最新週報為 08-28 | 本會 | — | 待確認資料來源 |
| `feedback.html`、`system-board.html` 兩條失效連結 | 本會 | — | 搬移前既有問題 |
| 首頁快速搜尋按鈕位置需調整 | 本會 | — | non-blocking，列入下次優化批次 |
| AI顧問頁面架構方向（維持雙欄局部優化 vs 單欄循序流程） | Alex | — | 已示範兩版DEMO，PPC決定暫緩，優先度後排 |
| `target_scale` 格式統一（`51-100人` vs `51~100人`等連字號/波浪號不一致） | 本會 | — | 「規模區間壓縮顯示」長期方案前置作業 |
| `target_scale` 官方標準選項清單確認（含`100人以上`與`101~200人`關係釐清） | 本會 | — | 同上，需人工確認官方表單定義 |
| 方案「適用規模」改為連續區間壓縮顯示（如「50人以下」） | 本會 | — | 長期方案，待上兩項前置作業完成後才能寫規格書 |
| API身分驗證 Phase 2（個人帳號登入） | PPC | — | Phase 1已完成並merge（PR #147），Phase 2待獨立規劃 |
| CSP第二波（Content-Security-Policy正式啟用） | 本會 | — | 已查證尚未啟動，7/13報告已規劃，待排入規格書 |
| 網站存活監控告警機制 | 本會 | — | 目前依賴人工發現異常 |
| `data_sources`表拆分「主辦單位」與「執行單位」欄位 | 本會 | — | 9/7排查連結時發現，非急迫 |
| `diagnosis.html`下架（導向至manufacturing.html） | 本會 | — | 9/9確認為manufacturing.html廢案前身，無內部連結指向，待排入 |
| `solution_reviews`資料表建立確認 | 本會 | — | 9/10已貼規格書給Codex，尚未收到最終SYNC確認 |
| 9月移轉後重點盤點文件上線 | PPC | — | `docs/migration_monthly_review_20260910.html`與`index.html`修改內容已備妥，尚未commit至GitHub |

> 註：「資料庫備份可還原性演練」已於 2026-09-10 完成並通過，自本表移除。
